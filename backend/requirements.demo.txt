# Minimal requirements for running demo mode locally (no Docker, no Playwright, no AWS)
# Install with: pip install -r requirements.demo.txt

# Core Framework
fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.0
pydantic-settings==2.2.1

# Database
sqlalchemy==2.0.30
asyncpg==0.29.0
aiosqlite==0.20.0

# Auth & Security
python-jose[cryptography]==3.3.0
passlib==1.7.4
bcrypt==4.0.1
cryptography==42.0.7
python-multipart==0.0.9

# Utilities
python-dotenv==1.0.1
httpx==0.27.0
aiofiles==23.2.1
python-dateutil==2.9.0
pytz==2024.1

# Payments (needed for subscription endpoints — razorpay's client.py needs pkg_resources,
# which setuptools>=81 removed)
setuptools<81
razorpay==1.4.2

# Excel parsing (used only by gst/excel_parser.py's optional file-upload
# feature — imported lazily, so omitting them here doesn't break demo mode
# core flows. Uncomment if you need to test real Excel/CSV invoice upload:
# pandas==2.2.2
# openpyxl==3.1.2

# AI (needed for type checks, actual calls are mocked in demo)
anthropic==0.28.0
